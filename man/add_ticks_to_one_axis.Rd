% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-ticks-to-one-axis.R
\name{add_ticks_to_one_axis}
\alias{add_ticks_to_one_axis}
\title{Add ticks and labels to one axis}
\usage{
add_ticks_to_one_axis(
  log_scale,
  x_or_y,
  tcl_small,
  mgp_val,
  big_ticks,
  big_ticks_labels,
  small_ticks,
  small_ticks_by,
  small_ticks_labels
)
}
\arguments{
\item{log_scale}{logical, whether not this axis is on the log10 scale}

\item{x_or_y}{character, either "x" or "y" to specify which axis the tick
marks and labels are being added to}

\item{tcl_small}{Length of small tickmarks; see \link{?axis}.}

\item{mgp_val}{mgp values to use, as in \code{plot(..., mgp = mgp_vals)}; see \link{?par}.}

\item{big_ticks, }{big_ticks_labels, small_ticks, small_ticks_by,
small_ticks_labels See corresponding \code{x_...} definitions in
\code{\link[=plot.size_spectrum_numeric]{plot.size_spectrum_numeric()}}.}
}
\value{
adds tickmarks to one axis of a plot. Returns invisible.
}
\description{
Called from \code{\link[=add_ticks]{add_ticks()}} which is called from \code{\link[=plot.size_spectrum_numeric]{plot.size_spectrum_numeric()}}.
}
\examples{
\dontrun{
res_vec <- fit_size_spectrum(sim_vec)
plot(res_vec)
plot(res_vec, log = "x")
plot(res_vec, log = "")
plot(res_vec, x_small_ticks_labels = c(5, 50, 500), log = "x") # Tailor the
  labels for a particular figure
}
}
\author{
Andrew Edwards
}
